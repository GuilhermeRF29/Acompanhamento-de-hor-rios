import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.controle.partidas',
  appName: ' Controle de Partidas',
  webDir: 'dist'
};

export default config;
